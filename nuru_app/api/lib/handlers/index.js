const handleAuth = require('./auth');
const handleUsers = require('./users');
const handleCourses = require('./courses');
const handleAssignments = require('./assignments');

module.exports = {
  handleAuth,
  handleUsers,
  handleCourses,
  handleAssignments
};