{
  "name": "nuru-foundation-monorepo",
  "private": true,
  "workspaces": [
    "backend"
  ],
  "scripts": {
    "vercel-build": "cd backend && npm install && npx prisma generate --schema=./prisma/schema.prisma"
  }
}